# Leave empty to use the explicitly labelled local demo snapshot.
# When a backend exists, point this to its public origin, for example:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=

# AI Radar 产品路线图

## 文档定位

- 更新时间：2026-09-05
- 性质：产品方向，不授予数据写入、模型调用、部署或发布权限
- 主定位：基于可验证的最新变化辅助 AI 模型与产品选择

工程执行顺序和上线门槛以 `docs/PROJECT_COMPLETION_SPEC.md` 为准。

## 产品设计规划

### 1. 北极星任务

AI Radar 服务于一个连续任务：用户先发现值得关注的 AI 变化，再理解变化背后的实体、关系与证据，最后结合自己的任务和约束形成可复查的选择。

```text
发现变化 → 理解关系与 Evidence → 提交决策约束 → 获得有条件的建议 → 保存或继续核验
```

产品成功不以页面浏览量、模型数量或生成文本长度衡量。核心成功信号是用户能回答三个问题：发生了什么、这条信息为什么可信、它如何改变我的选择。

### 2. 用户与关键场景

| 用户                | 到达产品时的状态                       | 核心任务                                  | 成功结果                       |
| ------------------- | -------------------------------------- | ----------------------------------------- | ------------------------------ |
| 产品经理与创业团队  | 需要选择能力路线，但预算和上线时间有限 | 找到值得进入 PoC 的模型，并说明取舍       | 得到主选、备选、风险和验证清单 |
| 工程与架构负责人    | 已有性能、隐私、部署或接口约束         | 核对版本、规格、限制、生态和官方 Evidence | 排除不合格方案，保留可验证候选 |
| AI 研究与行业观察者 | 需要持续理解模型演进和厂商变化         | 发现重要变化并追溯版本、谱系、竞品和来源  | 建立可信、可复查的变化脉络     |

### 3. 产品架构与导航

一级导航只承载三个用户意图：

1. **动态**：高频发现入口，回答“最近有哪些变化值得我看”。
2. **决策助手**：核心任务入口，回答“基于我的任务、预算、部署和风险约束，该先评估谁”。
3. **知识库**：直接查找入口，回答“这个实体、版本或事实目前是什么状态”。

Compare、Graph、Evidence、质量页和账户能力保留为上下文工具。`/graph` 只能从实体关系区的“查看完整关系网络”进入；Compare 由决策结果或实体页发起；Evidence 必须贴近支撑它的 Claim，不成为孤立信息仓库。

### 4. 核心页面体验

#### 4.1 动态首页 `/`

首屏按“变化索引—事实汇聚—证据检查”的顺序组织。用户选择一条变化后，页面同步呈现发生了什么、影响对象、证据状态、相关实体以及进入决策助手的明确动作。首页展示 8 个核心模型，并将 GPT、Claude、Gemini 标记为 Anchor Models；该分层表示研究深度，不代表排名。

关键动作：查看变化详情、打开实体、检查 Evidence、带着当前对象进入决策助手。

#### 4.2 决策助手 `/ask`

决策助手采用“约束表单 + 可补充问题”的输入结构：

| 字段       | 必填 | 作用                                                   |
| ---------- | ---- | ------------------------------------------------------ |
| 任务       | 是   | 描述要完成的工作、输入输出和主要使用者                 |
| 首要优先级 | 是   | 从质量、成本、速度、隐私、可控性等维度确定主要取舍     |
| 预算       | 是   | 表达成本优先、预算区间或当前未知                       |
| 部署方式   | 是   | 云 API、私有部署、端侧、可混合或尚未确定               |
| 排除条件   | 否   | 记录数据出境、供应商、许可、上下文、延迟等不可接受条件 |
| 补充问题   | 否   | 允许用户说明行业、团队能力、现有技术栈和评估周期       |

结果不是单一排行榜，而是一份带时间口径的决策记录，固定包含：

1. **建议摘要**：主选、备选或“证据不足，暂不能推荐”。
2. **适用条件**：这项建议成立所依赖的任务和约束。
3. **取舍矩阵**：只比较当前 Evidence 能支撑的维度；缺失项显示未知。
4. **关键依据**：每项判断关联 Claim、Evidence、信息日期和可信状态。
5. **风险与冲突**：显示限制、相互矛盾的来源和可能过期的信息。
6. **下一步验证**：给出 PoC、人工确认或补充信息清单。

决策结果中的事实、推断、未知与冲突必须在文案和视觉状态上都可区分。任何无法追溯到 Verified Claim / Evidence 的内容不能伪装成事实。

#### 4.3 知识库 `/knowledge` 与实体页

知识库先帮助用户区分模型系列、具体版本、厂商和其他实体，再进入档案。实体页使用统一顺序：身份与当前状态、关键变化、已核验事实、可读关系、时间线、Evidence。模型页不得再出现第二套重复时间线。

关系区直接回答“由谁开发、属于什么系列、前后版本、主要替代和生态连接”，每条关系携带方向、状态和来源。图谱仅用于路径、邻域和时间过滤等高级探索；移动端优先提供关系列表。

#### 4.4 三种阅读模式

阅读模式共享同一套 Verified Claim、Evidence 和可信状态，只调整内容选择、排序、解释重点和默认展开层级：

| 模式 | 首先回答                               | 默认突出                                       | 默认收起                   |
| ---- | -------------------------------------- | ---------------------------------------------- | -------------------------- |
| 通俗 | 发生了什么、为什么重要、对用户有何影响 | 变化摘要、影响、常见场景、关键限制             | 规格表、完整来源和高级比较 |
| 产品 | 应用于什么场景、商业和竞品判断如何变化 | 用户场景、竞品差异、价格与限制、产品机会和风险 | 底层参数与完整技术证据     |
| 技术 | 能力如何实现、接口与边界是什么         | 规格、API、Benchmark、技术限制、完整 Evidence  | 通俗解释和非技术背景       |

切换模式后，首屏标题、前三个内容区及 Evidence 展开状态应立即发生可感知变化；URL 或用户偏好应允许刷新后保持当前模式。

### 5. 跨页面闭环

每个核心页面都要提供上下文连续的下一步：

- 动态 → 实体：携带所选变化或事件上下文。
- 动态 / 实体 → 决策助手：预填候选实体和已知任务线索，不替用户猜预算或部署方式。
- 决策助手 → Compare：只带入结果中的主选与备选，并沿用比较维度和信息日期。
- 决策助手 → Evidence：直接定位支撑具体判断的来源。
- 研究记录 → 实体 / 动态：允许用户复核结论之后发生的变化。

浏览器前进、返回和分享链接应保留关键上下文；缺少上下文时页面提供明确恢复入口。

### 6. 状态、响应式与无障碍

- Loading 使用与最终信息结构一致的骨架，避免布局跳变。
- Empty 解释为什么没有结果，并提供修改筛选、返回核心模型或补充约束的动作。
- Error 区分网络失败、权限不足、Evidence 不足和系统拒答，保留用户已输入内容。
- Demo、Live、未核验、冲突和过期状态同时使用文字与非颜色标记。
- 桌面支持并列索引、工作区和 Evidence 检查；移动端按任务顺序堆叠，底部导航固定为三个一级入口。
- 所有切换器、时间线点、关系项、折叠区和对话框支持键盘操作、清晰焦点和正确 ARIA 状态；减少动态偏好下取消非必要运动。

### 7. 内容与数据策略

Core Model Universe 首批固定为 GPT、Claude、Gemini、DeepSeek、Qwen、Kimi、Doubao、ERNIE。Anchor Models 优先补足完整演进、关键版本差异、能力、限制、价格、部署和生态 Evidence；其他核心模型达到可追踪、可比较、可进入决策助手的深度。

长尾实体仅在真实任务或高价值 Evidence 驱动时扩展。内容团队以“这条信息是否改变选择”为收录判断，不使用事件数、关系数或模型数作为生产配额。

### 8. 验证指标

正式产品验证以 5–10 位目标用户的真实任务为基础：

- 30 秒定位测试：能否说清追踪器、决策助手和证据基础设施各自作用。
- 决策完成率：能否提交完整约束并理解主选、备选与拒答。
- 证据可达性：能否从一项建议在两次操作内到达支撑 Evidence。
- 事实辨识：能否区分事实、推断、未知和冲突。
- 找证据耗时：从看到结论到确认来源所需时间。
- 跨端任务完成：同一核心流程在桌面和 390px 真实移动视口均可完成。

这些指标用于发现问题和比较迭代，不作为制造数据或降低证据标准的理由。

## Phase 0：可信发布基线

- [ ] 处理疑似凭证暴露事件。
- [ ] 自动抽取恢复为默认关闭。
- [ ] 当前 UI 回归修复通过真实浏览器验收。
- [ ] GitHub Quality 全绿。
- [ ] Render 与 Cloudflare 运行同一精确提交。
- [ ] staging smoke 通过。

完成标准不是“已提交”或“已开始部署”，而是运行环境和验收证据一致。

## Phase 1：统一核心阅读体验

- [ ] 一级导航只保留动态、决策助手和知识库；Graph 降级为实体页的高级探索入口。
- [ ] 模型页只保留一套时间线呈现。
- [ ] 三种阅读模式在信息集合、排序和解释重点上有真实差异，不只是视觉样式不同。
- [ ] 通用实体页与模型页使用一致的阅读组件。
- [ ] 实体页先展示可读关系，再按需进入完整关系网络。
- [ ] 时间线、关系、Evidence 和决策入口在桌面与手机均可用。
- [ ] 关键交互由渲染测试或浏览器测试覆盖。

验收：用户能从实体页完成“理解变化 → 查看证据 → 比较选择 → 继续研究”的完整流程。

## Phase 2：分层核心模型的证据深度

先建立 8–10 个 Core Model Universe；当前以 GPT、Claude、Gemini、DeepSeek、Qwen、Kimi、Doubao、ERNIE 为第一组。GPT、Claude、Gemini 是 Anchor Models，优先做深；其他核心模型保证足以支持追踪、横向比较和决策助手。长尾实体只在 Evidence 有价值时收录。

每个主题应尽量覆盖：

- 当前定位与适用场景；
- 关键版本和变化；
- 已核验能力与限制；
- 与替代方案的可证据化差异；
- 直接官方来源；
- Evidence 不足时的明确空白。

验收：内容能帮助完成具体选型任务，且每个事实都可追溯；没有为满足条数而生成的内容。

## Phase 3：决策助手与对比

- [ ] 决策问题显式包含任务、优先级、预算、部署和排除条件。
- [ ] 建议明确区分事实、推断、未核验和冲突，并说明适用条件。
- [ ] 对比页作为决策助手的二级结构化工具，展示相同维度、时间口径和来源。
- [ ] Evidence 不足时拒答或缩小问题，不补写缺失结论。
- [ ] 检索诊断对管理员可见，对普通用户保持易懂。

验收：目标用户能更快找到可信依据，而不是只看到更多文本。

## Phase 4：持续追踪

前置条件：信源采集、审核、发布和回滚已稳定运行。

- [ ] 最近变化按时间和重要性呈现。
- [ ] 每条变化显示来源、新鲜度和影响范围。
- [ ] 信源健康状态和失败恢复可观察。
- [ ] 根据真实需求决定是否启用关注、摘要和外部邮件。

不把“每天必须新增内容”设为目标；没有重要变化时，保持不变是正确结果。

## Phase 5：正式产品验证

- [ ] 完成 5–10 位目标用户的任务测试。
- [ ] 记录找证据耗时、任务完成率、误解点和拒答质量。
- [ ] 明确定义 production 分支、服务、数据库、域名、监控、备份和回滚责任。
- [ ] 机器门禁和外部人工检查均通过后，再决定是否切换 `live`。

## 决策原则

1. 证据质量优先于内容数量。
2. 用户任务优先于功能数量。
3. 同一事实只有一个权威状态来源。
4. 产品目标不得绕过安全、审核和 live gate。
5. 所有路线图状态必须链接到可验证结果；没有证据就保持未完成。

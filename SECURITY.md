# AI Radar 安全策略

## 支持范围

当前安全修复以默认分支上的最新版本为准。预发布环境用于作品集演示和生产化验证，不承诺正式生产服务等级，但不会因此降低凭据保护、鉴权、数据隔离和证据发布门槛。

## 私密报告漏洞

请优先通过 GitHub 仓库的 Security Advisory 私密报告功能提交问题。不要在公开 Issue、Discussion、Pull Request、截图或日志中粘贴以下内容：

- API Key、访问令牌、JWT、Cookie 或密码；
- Neon/PostgreSQL 完整连接串；
- Render、Cloudflare、GitHub 或邮件服务凭据；
- 可识别真实用户的私密研究记录；
- 尚未修复漏洞的完整利用步骤。

报告建议包含受影响版本、复现条件、预期影响和经过脱敏的日志。仓库维护者确认收到后，会先评估影响和临时缓解措施，再准备修复与披露说明。

## 安全边界

- LLM 只能创建候选事实，不能直接写入公开知识库。
- 公开 Claim 必须经过审核并关联直接 Evidence。
- 管理接口使用 JWT/RBAC；静态管理员令牌仅用于首次初始化或应急恢复。
- 自动化任务使用独立令牌，不能替代管理员身份。
- 真实 `.env`、数据库、备份和平台凭据均不得提交到仓库。

如果报告中意外包含真实凭据，应立即在对应供应商控制台撤销并轮换，而不是等待代码修复完成。

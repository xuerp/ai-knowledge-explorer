# Spec 覆盖与交付边界

## 已实现并可验证

| Spec 能力  | 实现                                                           |
| ---------- | -------------------------------------------------------------- |
| 发现模式   | 首页、知识库、实体详情、系列/具体版本比较、关系查询图谱        |
| 阅读与语言 | 中文/English，通俗/产品/技术，明暗主题                         |
| 时间和证据 | Claim 双时间字段、Evidence 引用、可信状态、时间线              |
| 数据流水线 | 官方信源、安全采集、快照、哈希去重、Diff、调度                 |
| AI 抽取    | 严格 JSON Schema 供应商适配器；输出固定 unverified             |
| 实体与冲突 | 别名消歧、结构化 subject/predicate/value 冲突和有效期重叠检测  |
| 人工审核   | JWT/RBAC、队列、理由、版本冲突、批准/拒绝、审计、发布历史      |
| 公开边界   | pending/rejected/needs-more-evidence Claim 从公共快照隔离      |
| 用户能力   | 登录、持久化关注、站内通知、已读状态、每日摘要偏好             |
| 研究       | 已审核 Claim 检索、证据不足拒答、私密记录、主动公开、Markdown  |
| 通知和邮件 | 相关关注者通知、Outbox、可选 SMTP 投递                         |
| 工程       | PostgreSQL 驱动、Alembic、Docker Compose、CI、PWA、生产构建    |
| 可扩展目录 | Entity、具体模型版本、关系与时间线持久化；受保护管理员增量写入 |
| 验收工具   | 数据质量 API、20 个黄金研究问题、自动测试                      |

## 演示与真实能力的区分

- `/admin/review-demo` 是明确标记的只读演示；未登录的 `/following` 和 `/ask` 也会保留明确的演示引导。
- 已登录且配置 API 时，`/account`、`/following`、`/ask`、私密研究详情和 `/admin/review` 均连接真实后端。
- `AI_RADAR_DATA_MODE=demo` 时公共 API 仍明确返回 demo/cached。
- 抽取、邮件和云部署只有在配置真实外部服务后才会执行。

## 当前正式数据差距

Spec 要求正式图谱约 40–50 个有效实体、150 条已审核 Claim、核心对象至少 5 条关系，并且 20 个黄金问题至少 85% 通过。

仓库当前演示目录为 49 个实体、70 条带来源关系、30 个来源、53 条时间线事件和 14 条带来源 Claim（其中 13 条可公开读取）。目录包含 8 个模型系列、19 个具体版本、5 个 Agent 产品，以及 LangGraph、AutoGen、CrewAI、OpenAI Agents SDK 等框架节点。它用于验证产品和工程闭环，不属于正式数据验收。`GET /api/v2/admin/data-quality` 会机器化报告尚未达到 150 条人工审核 Claim、以及核心节点关系覆盖不足等差距，避免人为误报。

完成正式验收仍需：

1. 选择并核验真实官方信源。
2. 配置抽取供应商，生成候选而非事实。
3. 人工逐条核验 Claim、关系、有效期和引用。
4. 扩充到数据门槛并运行 20 个黄金问题。
5. 由产品/研究负责人签署数据验收。

## 外部交付边界

代码已准备好，但以下需要用户的外部账号、密钥或决策：

- Cloudflare/Lovable 和 API 云主机访问权。
- 生产 PostgreSQL、域名、HTTPS、日志、告警和备份。
- 模型供应商密钥与预算。
- SMTP/事务邮件供应商与已验证发件域。

因此，本仓库能完成的是可复现、可测试、可部署的系统实现；不能在没有这些资源时声称已经公开上线或完成正式数据研究。
